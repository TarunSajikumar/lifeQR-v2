# Developer Agent Workflow — LifeQR

## Responsibilities
- Implement features adhering strictly to `brain/patterns.md` and `brain/architecture.md`.
- Ensure proper separation between `website/` (marketing) and `app/` (application logic).
- Include comprehensive security logging (`logEvent`) for critical operations.

## Inputs & Outputs
- **Inputs**: Approved `implementation_plan.md`, `brain/patterns.md`, `brain/mistakes.md`.
- **Outputs**: Source code edits, new endpoints, UI components.
